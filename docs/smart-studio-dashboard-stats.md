# Smart Studio — ספק כרטיסי הסטטיסטיקה בדשבורד

> **מה זה:** ההגדרה המדויקת של כרטיסי הסטטיסטיקה במסך Smart Studio (`/studio`) —
> המספר, הכיתוב, שורת ההסבר, האייקון, ומקור הנתון (אמת חיה מול עובדת-פרוייקט
> שהוכתבה). המסמך נכתב **במקביל** לסשן שבונה מחדש את מסך הסטודיו
> (`claude/smart-studio-learning-ely9gw`), ומזין את הרבנייה בלי לגעת בקוד שנבנה
> עכשיו. זה ספק-תוכן, לא מימוש.
>
> **טקסט הכרטיסים באנגלית** — כמו המסך עצמו (מסך הסטודיו אנגלית). נרטיב המסמך
> נשאר עברית (זה ספק פנימי); מה שמופיע על המסך הוא רק העמודות `Number` /
> `Label` / `Sub-line`.
>
> **סטטוס:** טיוטה לאישור · 2026-07-29 (ניו יורק) · ענף `claude/smart-studio-parallel-work-ctc4lx`.

## תבנית הכרטיס (הכתבת המשתמש)

כל כרטיס = **מספר גדול** · **כיתוב מודגש (2–3 מילים)** · *שורת הסבר (רק אם צריך)*.
זו תבנית כרטיסי `/admin` היום (`src/app/[locale]/(app)/(platform)/admin/page.tsx`):
כותרת קטנה־אפורה, ערך גדול ומודגש, תת-שורה. אייקונים מ-**lucide-react**.
כלל: **"X of Y" בכל מקום עם מכנה אמיתי**. **רשימה אחת שטוחה — בלי חלוקה לקבוצות.**

## הכרטיסים (טקסט אנגלי — כפי שיופיע על המסך)

| # | Number | **Label** | Sub-line | אייקון (lucide) | מקור הנתון |
|---|---|---|---|---|---|
| 1 | **1,360 / 3,400** | Hours logged | of the full-project estimate | `Hourglass` | עובדת-פרוייקט (הוכתב) |
| 2 | **3 / 7** | Team hired | — | `UserPlus` | עובדת-פרוייקט (הוכתב) |
| 3 | **3** | Months in | invested so far | `CalendarDays` | עובדת-פרוייקט (הוכתב) |
| 4 | **~4** | Months to launch | est. until production starts | `Rocket` | עובדת-פרוייקט (הוכתב) |
| 5 | **1,394** | Models scanned | *(אין שורת הסבר)* | `ScanSearch` | **אמת חיה** |
| 6 | **23** | Research completed | across all pipeline stages | `FlaskConical` | **אמת חיה** |
| 7 | **30** | Methods built | how to drive each model | `Wrench` | **אמת חיה** |
| 8 | **100** | New programs | planned in the coming year | `Film` | עובדת-פרוייקט (הוכתב) |
| 9 | **$2,000** | Cost per program | end-to-end, incl. marketing | `Coins` | עובדת-פרוייקט (הוכתב) |
| 10 | **3** | New audiences | — | `Target` | עובדת-פרוייקט (הוכתב) |

> **i18n:** לפי מוסכמת הרפו כל מחרוזת עוברת `useTranslations` ונשמרת כמפתח.
> מכיוון שהמסך אנגלית-בלבד, מוסיפים את המפתחות ל-`src/messages/en.json` (ולפי
> הצורך גם `he.json`) באותו קומיט שמשתמש בהם.

---

## מאיפה מגיע כל מספר "אמת חיה" (מאומת 2026-07-29)

שלושת הנתונים המסומנים "אמת חיה" נבדקו מול הרפו `video-lab`, לא מהזיכרון:

| נתון | ערך | מקור מאומת |
|---|---|---|
| Models scanned | **1,394** | `docs/models/catalog.json` (‎`stats`‎, 1,394 מודלים) · `docs/research-index.md` טבלת "היקף הסריקה" (1,394/1,394; 1,388 סכמות; 13,221 שדות) |
| Research completed | **23** | `docs/research-index.md` § "המחקרים — לפי שלב": שלב 03 (6) · 04 (4) · 05–06 (1) · 07 (4) · 08 (2) · חוצי-שלב (6) = 23. כולל **כל** מה שנעשה (2 סריקות-קטלוג מוקדמות הוחלפו אך נספרות). |
| Methods built | **30** | `docs/research-index.md` טבלת "היקף הסריקה" → "מתכוני מודל כתובים: 30" (‎`docs/models/*.md`‎, למעט README/prices-memo/havara/sub-research/voice-memo) |

**המלצת מימוש:** שלושת אלה יכולים להתעדכן **חי** — לחשב ב-build/endpoint מתוך
`video-lab` (או מטבלת ההרצות ב-DB) במקום לקבע ידנית, כך שהמספר לא מתיישן. שבעת
האחרים הם **עובדות-פרוייקט שהוכתבו** ונקבעים ידנית (קונפיג/DB) עד שיהיה מקור אוטומטי.

## החלטות שננעלו

1. **טקסט הכרטיסים אנגלית** (כמו המסך).
2. **רשימה אחת שטוחה** — בלי חלוקה לקבוצות.
3. **כרטיס "Models scanned" בלי שורת הסבר.**
4. **חודשים — שני כרטיסים נפרדים** (Months in · Months to launch).
5. **Research = 23** ("כל מה שנעשה", לא 21).
6. **New programs / New audiences — היעד בלבד** (100, 3), לא "0 of".

## פתוח / הנחות

- **כרטיס תקציב ($200,000 = 100 × $2,000)** — לא נכלל (לא הוכתב). קל להוסיף.
- הבהרת "3. 3" בצ'אט טרם פוענחה — לא השפיעה על אף כרטיס.
